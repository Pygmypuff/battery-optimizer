nordpool==0.5.1
# scipy==1.14.1 (see below) requires numpy<2.3, so this can't float up to
# the latest numpy independently — `pip install -r requirements.txt` will
# hard-fail with a ResolutionImpossible error if this and scipy drift out
# of sync. Verified together via `pip check` in a clean venv.
numpy==2.2.4
openpyxl==3.1.5
pandas==3.0.2
platformdirs==4.11.3
PySide6==6.11.1
pytz==2026.1.post1
# Pinned, not just >=1.9.0 (the minimum for scipy.optimize.milp): scipy
# 1.18.0 was verified to hang on `import scipy.optimize` specifically in a
# PyInstaller-frozen build (confirmed via isolated testing — plain `import
# scipy` was fine, only `scipy.optimize` hung; unfrozen it imported in
# under a second). 1.14.1 + numpy==2.2.4 above was verified clean in the
# same frozen-build test, via a clean `pip install -r requirements.txt`
# (not just an ad-hoc `pip install scipy==...` over an existing env, which
# is how the numpy conflict here slipped through the first time), and
# produces identical optimizer output. Re-test both before bumping either.
scipy==1.14.1
